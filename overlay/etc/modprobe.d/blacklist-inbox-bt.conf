# Unitree G1 carrier — the in-box btusb/btrtl drivers don't recognise the 8852B
# BT IC (they bring hci0 up with a 00:00:00:00:00:00 address and no firmware).
# Blacklist them so Realtek's rtk_btusb owns the BT USB interface instead.
blacklist btusb
blacklist btrtl
